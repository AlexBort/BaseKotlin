package com.example.basekotlin

open class Mouse { // I shouldn't add modifier 'public' because it has such access by default

    private fun privateFun() { // private access for function
    }

   protected fun funA() {}
}
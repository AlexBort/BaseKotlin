package com.example.basekotlin

enum class Colors(val colorCode: String, val opacity: Double? = 1.0) {
    // enum - это перечисление констатных значений, которые мы знаем зраннее чему они равны

    WHITE(colorCode = "",2.0),
    BLACK(colorCode = ""),
    RED(colorCode = ""),
    GREEN(colorCode = "")
}